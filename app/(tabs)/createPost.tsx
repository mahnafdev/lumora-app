import { View, Text } from "react-native";

export default function CreatePost() {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: "center",
				alignItems: "center",
			}}
		>
			<Text style={{ fontSize: 24 }}>Create Post</Text>
		</View>
	);
}
